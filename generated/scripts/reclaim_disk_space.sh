#!/bin/bash
# Disk Space Prophet — Safe Cleanup Script
# Each command is commented — remove # to execute

# rm -rf ~/.cache  # 4.9G
# rm -rf ~/.npm  # 9.7G

# Clear npm cache
# npm cache clean --force

# Clear pip cache
# pip cache purge

# Clear brew caches
# brew cleanup --prune=7

# Clear macOS derived data
# rm -rf ~/Library/Developer/Xcode/DerivedData